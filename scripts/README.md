# scripts

Helper scripts for humans (repo bootstrap, pre-commit checks, local validation). Terraform itself must never shell out to anything here — see the root README for the local-exec/remote-exec justification policy.
