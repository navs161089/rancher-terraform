# examples

Standalone example root modules showing how to consume this repo's modules from a different environment (e.g. a 3-server/N-worker HA layout, or a cloud target). Added once the local lab is stable.
